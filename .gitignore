# Terraformの状態ファイル（state管理用、機密情報含むため除外）
*.tfstate
*.tfstate.backup
*.tfstate.*

# Terraformのロックファイル（依存関係管理用）
*.terraform.lock.hcl

# 環境ごとの設定ファイル（機密情報や環境変数を含む場合がある）
config/dev.yml
config/prod.yml

# Terraformのクラッシュログ（デバッグ用）
crash.log

# Terraformのplanファイル（一時的な実行計画）
*.tfplan

# Terraformの変数・オーバーライドファイル（環境ごとの値や一時的な設定）
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# プロバイダー認証情報（AWS等の認証情報ファイル）
.terraformrc
terraform.rc

# Terraformのプラグイン・プロバイダー格納ディレクトリ
.terraform/

# エディタの一時ファイル・バックアップファイル
*~
*.swp
*.bak

# VS Codeの設定ディレクトリ（個人環境用）
.vscode/

# macOSのシステムファイル
.DS_Store

# AWS認証情報・鍵ファイル
*.pem
*.key
.aws/

# Lambdaのビルド成果物（ZIPや一時ディレクトリ）
terraform/modules/lambda_ec2_launch_ansible_trigger/build/
terraform/modules/lambda_ec2_launch_ansible_trigger/lambda_layer.zip

# Ansibleのキャッシュ・リトライファイル
*.retry
.cache/

